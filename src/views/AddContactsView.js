import React from "react";
import ContactForm from "../components/ContactForm";

const AddContactsView = () => (
  <div className="centeredDiv">
    <h1>Add contact</h1>
    <ContactForm />
  </div>
);

export default AddContactsView;
